<!DOCTYPE html>
<html lang="vi" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lịch sử thi</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="navbar navbar-expand-lg bg-body-tertiary border-bottom sticky-top">
    <div class="container-fluid">
      <a href="/" class="btn btn-secondary">Trang chủ</a>
      <h2 class="h5 mb-0">Lịch sử bài thi</h2>
      <span></span>
    </div>
  </header>

  <main class="container mt-4">
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="h3">Chi tiết Lịch sử Bài thi</h2>
        
        <form action="/history/clear" method="POST" onsubmit="return confirm('Bạn có chắc chắn muốn xóa TẤT CẢ lịch sử bài thi không? Hành động này không thể hoàn tác.');">
            <button type="submit" class="btn btn-danger">Xóa TẤT CẢ Lịch sử</button>
        </form>
    </div>

    <% if (history.length === 0) { %>
      <div class="alert alert-info" role="alert">
        Chưa có bài thi nào được ghi lại trong lịch sử.
      </div>
    <% } else { %>
      <div class="table-responsive">
        <table class="table table-dark table-striped table-hover">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Thời gian thi</th>
              <th scope="col">Điểm số</th>
              <th scope="col">Hành động</th>
            </tr>
          </thead>
          <tbody>
            <% history.forEach((h, index) => { %>
              <tr>
                <th scope="row"><%= index + 1 %></th>
                <td data-timestamp="<%= h.timestamp %>"><%= formatTimestamp(h.timestamp) %></td>
                <td><%= h.score %>%</td>
                <td>
                  <a href="/history/<%= h.id %>" class="btn btn-sm btn-info">Xem chi tiết</a>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    <% } %>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>